!!!
html
  head
    title #{laugh}
  body
    bar = 'sur'+'vive'
    // this is a comment
    h1#hdr(background="foo(1) &dquot;hah!&dquot;").
      | Bar #{"'These quotes should \"" + bar + '"\', he said'} hopefully
      //
	  multiline
	  comment
    p.fred.fly
      | This is some text which
      | shows a multi-line example
    p.multi.line.mode
      |
	This is some more text which
	   illustrates indented multi-line mode
	  foo
    .foo#xid divtext
